<%# TODO: Typo on hook %>
<% entries = call_hook(:plugin_personal_timesheet_view_personal_timesheets_context_menu, { :time_entries => @time_entries }) %>

<% entries += call_hook(:plugin_personal_timesheet_views_personal_timesheets_context_menu, { :time_entries => @time_entries }) %>

<%= content_tag(:ul, entries) unless entries.empty? %>