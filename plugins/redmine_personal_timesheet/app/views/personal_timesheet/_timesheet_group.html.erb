<table class="<%= cycle("odd", "even") %> issue-time-entry-<%= entry -%> hascontextmenu"
       style="display:none;">
        <thead>
        <th width="2%">
         </th>
       
  </thead>
<% arr2 = []
arr2 << entry %>
  
<%= render :partial => "by_date", :collection => @timesheet.projects,
  :locals => {:current_date => entry} %>
  </table >
